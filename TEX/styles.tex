
% Ici, le fichier pour les styles
\startenvironment styles
\setupwhitespace[big]

\environment typo


\mainlanguage[fr]

% UTF-8 
    \enableregime[utf-8]              % Enters unicode 
    %\setupencoding[default=uc]       % Uses unicode fonts only

    \setuppagenumbering [alternative=doublesided]
 
% MARGES/FORMAT
    %format
    \definepapersize [format] [width=115mm, height=175mm]
    \setuppapersize [format] [format]
    
    \setcharacterspacing [frenchpunctuation]
   
    %marges
    \setuplayout[ 
        backspace=20mm,
        cutspace=10mm,
        topspace=10mm,
        bottomspace=10mm,
        width=85mm,
        height=155mm,
        leftmargin=5mm,
        rightmargin=5mm,
        footer=7mm,
        header=8mm, % removes header
        grid=yes, % activates baseline grid
        ]
        
 %alinéa
 \setupheadertexts[\hfill left(odd) header][] [][right(even) header
\hfill]

    
    %IMAGES
%\defineexternalfigure[nomimg][test][width=3cm,background=color,backgroundcolor=green]
%execution image + determine sa taille maximum
%\setupexternalfigures[maxwidth=110mm]

    
       %TYPO
    %  \setupbodyfont[logosssb, 11pt]
      \definebodyfontenvironment[default][a=1.5, b=2, c=4, d=6, xx=0.5, ] % defines higher sizes of fonts
% \setupinterlinespace[10pt]
       

    %MISE EN PAGE
    %titretexte

   %sous-titre
            \setuphead[subject][
            align={nothyphenated, verytolerant, center},
            aligntitle=float,
             style=\tfa
            ]
    %titrecourant (rappel de nom_chapitre)
        \setupheader[style=\tfxx]
      \setupheadertexts [{\getmarking[chapter]}]     [\tfx{Git, ce connard} ]
   
    %notes

    \setupnote[footnote][location=text, way=bypage, numberconversion=set 2, style={\bf}]
%    \setupnotetext[footnote][location=left, before={\startalignment[flushleft]}, after=\stopalignment] 


    
    %appel de notes
    %voir ficher texte
    
    %Nomdoeuvre
    %citations
    \def\citations#1%
    {
        {\logocit #1}
    }
    \def\logoregularitalic#1%
    {
        {\logoregit #1}
    }
   
     
    % folio
    \setuppagenumbering[location=footer, style={\setupbodyfont[10pt]}] 
   

   %JUSTIFICATION
    %\setuptolerance[verytolerant] % justification settings
   % \interlinepenalty=8000 % tries to avoid widows
    \widowpenalty=8000 % tries to avoid widows
    \clubpenalty=8000 % tries to avoid widows
    \doublehyphendemerits=1000000 % tries to avoid consecutive hyphenations
    %\setupfonthandling [hz] [min=50,max=50,step=10]
    %\setupalign[hz, hanging]
    
    %PAGE TITRE PROJET (CHAPITRE) 



\stopenvironment
